<odoo>
    <data>

        <record id="crm_rule_pricing" model="ir.rule">
            <field name="name">Pricing CRM Leads</field>
            <field ref="model_crm_lead" name="model_id"/>
            <field name="domain_force">[('stage_id.is_pricing_stage','=',True)]</field>
            <field name="groups" eval="[(4, ref('eit_freight_pricing.group_prcing_user'))]"/>
        </record>
    </data>
</odoo>